info:
  name: test
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8787/product/manage/me
  headers:
    - name: Authorization
      value: Bearer <shopToken>
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
