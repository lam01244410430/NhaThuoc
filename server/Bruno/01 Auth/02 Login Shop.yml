info:
  name: 02 Login Shop
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "identity": "nhathuocantam",
        "password": "Abcd@123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody()

        if (
          body?.data?.token &&
          body?.data?.user?.role === 'shop'
        ) {
          bru.setVar(
            'shopToken',
            body.data.token
          )

          console.log(
            'shopToken:',
            bru.getVar('shopToken')
          )
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
