info:
  name: 02 Login Admin
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "identity": "admin",
        "password": "admin"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody()

        if (
          body?.data?.token &&
          body?.data?.user?.role === 'admin'
        ) {
          bru.setVar(
            'adminToken',
            body.data.token
          )

          console.log(
            'adminToken:',
            bru.getVar('adminToken')
          )
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
