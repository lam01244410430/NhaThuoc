info:
  name: 03 Customer Me
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/auth/me"
  headers:
    - name: Authorization
      value: Bearer {{customerToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
