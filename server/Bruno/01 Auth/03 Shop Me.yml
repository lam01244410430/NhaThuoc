info:
  name: 03 Shop Me
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/shop/me"
  headers:
    - name: Authorization
      value: Bearer {{shopToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
