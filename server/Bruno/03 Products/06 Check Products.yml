info:
  name: 06 Check Products
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/product/{{productId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
