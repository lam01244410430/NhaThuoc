info:
  name: 05 Delete Product
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/product/{{productId}}"
  headers:
    - name: Authorization
      value: Bearer {{shopToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
