info:
  name: gọi shop
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/shops/{{shopId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
