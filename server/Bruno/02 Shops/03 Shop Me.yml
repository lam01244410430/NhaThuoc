info:
  name: 03 Shop Me
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/shops/me"
  headers:
    - name: Authorization
      value: Bearer {{shopToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
