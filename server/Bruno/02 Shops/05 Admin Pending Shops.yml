info:
  name: 05 Admin Pending Shops
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/shops/admin/pending"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
