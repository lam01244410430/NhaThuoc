info:
  name: Test Category
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/category"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
